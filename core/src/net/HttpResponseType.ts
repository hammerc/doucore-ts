namespace dou {
    /**
     * HTTP 返回值类型
     * @author wizardc
     */
    export enum HttpResponseType {
        arraybuffer = 1,
        blob,
        document,
        json,
        text
    }
}
